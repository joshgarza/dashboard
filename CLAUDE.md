# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## Linear
- Team: `JOS`
- Project: `dashboard`

## CLI

All commands via:
```bash
node --experimental-strip-types /home/josh/coding/claude/task-runner/main/src/cli.ts <command>
```

```bash
<cli> add-ticket "Title" --team JOS --project dashboard [--description "Details"]
<cli> run JOS-42
<cli> review <pr-url>
<cli> standup --project dashboard
```
